<div class="d-flex">
    {{ form_start(form) }}
        {{ form_row(form.q, {'attr': {'class': 'mt-3'}}) }}
        <button type="submit" class="btn btn-outline-success button-search mt-3 mx-3">Rechercher</button>
    {{ form_end(form) }}
</div>