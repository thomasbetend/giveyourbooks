{% extends 'base.html.twig' %}

{% block title %}Hello ConversationController!{% endblock %}

{% block body %}


<div class="container w-75">
    <h1 class="mt-4">Conversations</h1>
    {% for conversation in pagination %}
        {# <h2>{{ conversation.name}}</h2> #}
        {% for user in conversation.user %}
            {% if user != app.user %}
                <a href="{{ path('app_my_conversation', {id: conversation.id}) }}">
                    <div class="card col-12 bg-primary text-white">{{ user.pseudo|capitalize }}</div>
                </a>
            {% endif %}
        {% endfor %}
        {# {% for message in conversation.messages %}
            <p>{{ message.content}}</p>
        {% endfor %} #}
    {% endfor %}
</div>
{% endblock %}
